# @expo98/static-expo-config-reader

Typed transformation of the static Expo config reader from `legacy/expo98/dist/expo-ios.mjs`.

The module reads `app.json` directly or conservatively extracts simple values from the first `app.config.*` file without executing dynamic config code.

