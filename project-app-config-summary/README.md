# @expo98/project-app-config-summary

Typed transformation of Expo app config summary projection from `legacy/expo98/dist/expo-ios.mjs`.

The module projects app config metadata into the project-info payload and preserves the legacy static regex extraction used for `app.config.*` files.

