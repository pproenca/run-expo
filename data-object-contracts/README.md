# @expo98/data-object-contracts

Modernized primitive, record, and result DTO contracts for expo98.

This package exposes runtime-checkable constants and compatible TypeScript types
for the legacy `primitives.ts`, `records.ts`, and `results.ts` contract files.

## Verification

```bash
npm test
```

