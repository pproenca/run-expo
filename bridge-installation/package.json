{
  "name": "@expo98/bridge-installation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Transformed bridge installation, metadata, and health contracts for expo98.",
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": "./dist/main/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js"
  }
}
