# plugin-self-management-process-adapter Transformation Notes

Generated by `modernize-transform expo98 plugin-self-management-process-adapter TypeScript/Node 20 ESM`.

## Scope

This transform extracts the concrete subprocess composition needed by
`plugin-self-management` release checks. The command package owns release
fixture creation and predicates; this adapter supplies the real Node
`execFile` boundary that the final CLI adapter can inject.

## Mapping

| Behavior | Legacy Source | Modern Source | Rules |
| --- | --- | --- | --- |
| `releaseCheck` runs `process.execPath` with the CLI wrapper and release argv | `legacy/expo98/dist/expo-ios.mjs:8909-8916` | `src/main/index.ts:47` | RULE-021 |
| Release checks run outside the fixture with timeout `20000` and `rejectOnError: false` | `legacy/expo98/dist/expo-ios.mjs:8913-8916` | `src/main/index.ts:64` | RULE-021 |
| `execFilePromise` forwards cwd/env/timeout/maxBuffer to Node `execFile` | `legacy/expo98/dist/expo-ios.mjs:12009-12019` | `src/main/index.ts:64` | RULE-021 |
| Non-rejecting errors resolve `{ stdout, stderr, error }` instead of throwing | `legacy/expo98/dist/expo-ios.mjs:12023-12028` | `src/main/index.ts:83` | RULE-021 |
| Rejecting errors carry captured stdout/stderr for parity with the shared helper | `legacy/expo98/dist/expo-ios.mjs:12020-12022` | `src/main/index.ts:76` | RULE-021 |
| Runtime composition returns the `execFile`, `pluginRoot`, `homeDir`, and `tmpDir` dependency shape accepted by `plugin-self-management` | `modernized/expo98/plugin-self-management/src/main/index.ts:15-24` | `src/main/index.ts:49` | RULE-021 |

## Deliberate Deviations

| Deviation | Rationale |
| --- | --- |
| Node `execFile` and environment are injected rather than imported directly. | This matches the existing process-adapter pattern and keeps tests deterministic while final CLI composition owns runtime process policy. |
| The adapter returns the normalized `error` field even though `releaseCheck` currently ignores it. | The legacy `execFilePromise` returned the field, and keeping it available preserves useful diagnostic structure for future composition. |

## Not Migrated

| Legacy Area | Reason |
| --- | --- |
| Release fixture generation and JSON/help/version predicates. | Already transformed in `plugin-self-management`. |
| Generic command lookup and broad subprocess helper usage. | Covered by `command-runner-adapter`; this package is scoped to release-command composition. |

## Equivalence Tests

Characterization tests live in `src/test/characterization.test.ts` and cover:

- Release-shaped `execFile` dependency with `process.execPath`/CLI-wrapper argv.
- cwd, env, timeout, maxBuffer, and string coercion behavior.
- Non-rejecting release process failures with normalized error payloads.
- Rejecting error stdout/stderr attachment parity.
- Full runtime dependency object composition for `plugin-self-management`.
- Non-object error normalization.

Current result:

```text
6 passing, 0 failed, 0 skipped, 0 todo
```

## Follow-Ups

- Final CLI composition should pass `createPluginSelfManagementRuntimeDependencies(...)` into `plugin-self-management`.
