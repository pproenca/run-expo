# @expo98/project-filesystem-helpers

Typed transformation of expo98 project filesystem helpers from `legacy/expo98/dist/expo-ios.mjs`.

The module covers cwd normalization, project-root discovery, package-manager detection, JSON reads, file existence, first-match selection, and recursive file walking.

