# @expo98/ios-simulator-target-adapter

Typed transformation of the native iOS simulator target-list adapter from `legacy/expo98/dist/expo-ios.mjs`.

The module runs the legacy `xcrun simctl list devices available --json` command and normalizes simulator records for target discovery.

