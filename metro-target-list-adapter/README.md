# @expo98/metro-target-list-adapter

Typed transformation of the Metro target-list adapter behavior from `legacy/expo98/dist/expo-ios.mjs`.

The module fetches Metro `/json/list` from the legacy loopback URL and exposes the target-discovery fallback that treats unavailable Metro as an empty target list.

