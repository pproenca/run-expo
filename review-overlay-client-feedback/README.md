# @expo98/review-overlay-client-feedback

Modernized review overlay client feedback helpers extracted from
`legacy/expo98/dist/expo-ios.mjs`.

The package preserves point normalization, pointer/copy endpoint derivation,
clipboard POST behavior, inspector-data normalization, source parsing, and
feedback markdown formatting.

Run:

```bash
npm test
```
