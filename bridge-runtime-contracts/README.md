# @expo98/bridge-runtime-contracts

Modernized bridge runtime health expression and generated source contracts
extracted from `legacy/expo98/dist/expo-ios.mjs`.

The package preserves the legacy runtime expression used with
`Runtime.evaluate`, the bridge domain catalog, metadata payload, catalog-domain
projection, and the legacy generated registration source text.

Run:

```bash
npm test
```
