{
  "name": "@expo98/device-listing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Characterization tests for expo98 device listing behavior.",
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": "./dist/main/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js"
  }
}
