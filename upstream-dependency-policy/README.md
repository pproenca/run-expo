# @expo98/upstream-dependency-policy

Typed transformation of upstream dependency policy and version compatibility helpers from `legacy/expo98/dist/expo-ios.mjs`.

The module classifies Expo, React Native, Metro, DevTools, Rozenite, and Expo CLI dependency relationships without importing upstream internals.

