# @expo98/command-result-helpers

Typed transformation of common command validation and result formatting helpers from `legacy/expo98/dist/expo-ios.mjs`.

The module covers safe section envelopes, required string validation, numeric clamping, truncation, and error formatting.

