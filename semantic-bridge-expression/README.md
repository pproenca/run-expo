# @expo98/semantic-bridge-expression

Modernized semantic bridge `Runtime.evaluate` expression extracted from
`legacy/expo98/dist/expo-ios.mjs`.

The package builds the JavaScript expression used to discover an app-provided
semantic snapshot bridge through supported globals, validate the bridge version,
capture refs, and return the legacy payload shape.

Run:

```bash
npm test
```
