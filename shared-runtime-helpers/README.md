# @expo98/shared-runtime-helpers

Typed transformation of small shared helper behavior from `legacy/expo98/dist/expo-ios.mjs`.

The module covers optional string normalization, bundle-id process-name derivation, URL cookie redaction, and promise timeout fallback behavior.

