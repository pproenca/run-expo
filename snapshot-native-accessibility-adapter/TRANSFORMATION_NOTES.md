# snapshot-native-accessibility-adapter Transformation Notes

Generated by `modernize-transform expo98 snapshot-native-accessibility-adapter TypeScript/Node 20 ESM`.

## Scope

This transform extracts the native accessibility process boundary used by the
legacy `snapshotCommand` fallback after semantic bridge capture is unavailable.
It supplies the `findAxeCli` and `describeNativeUi` dependencies expected by
`snapshot-evidence`.

## Mapping

| Behavior | Legacy Source | Modern Source | Rules |
| --- | --- | --- | --- |
| Locate `axe` through `commandPath("axe")` | `legacy/expo98/dist/expo-ios.mjs:1586`, `legacy/expo98/dist/expo-ios.mjs:12001` | `src/main/index.ts:46` | RULE-010 |
| Run `axe describe-ui --udid <deviceId>` | `legacy/expo98/dist/expo-ios.mjs:1590` | `src/main/index.ts:50` | RULE-010 |
| Preserve timeout `12000`, max buffer `4 * 1024 * 1024`, and `rejectOnError: false` | `legacy/expo98/dist/expo-ios.mjs:1591` | `src/main/index.ts:52` | RULE-010, RULE-021 |
| Preserve command path lookup through `sh -lc "command -v <command>"`, timeout `5000`, and null when stdout is empty | `legacy/expo98/dist/expo-ios.mjs:12001` | `src/main/index.ts:61` | RULE-010 |
| Preserve normalized non-rejecting subprocess errors | `legacy/expo98/dist/expo-ios.mjs:12009` | `src/main/index.ts:70` | RULE-021 |

## Deliberate Deviations

| Deviation | Rationale |
| --- | --- |
| `execFile`, cwd, and env are injected instead of imported directly. | This matches the existing command-runner modernization pattern and keeps characterization tests deterministic. |
| This package does not parse `axe` JSON output. | `snapshot-evidence` owns native tree parsing, normalization, filtering, and persistence; this package owns only process execution. |

## Not Migrated

| Legacy Area | Reason |
| --- | --- |
| Semantic bridge capture and persistence. | Covered by `semantic-bridge-snapshot-adapter` and `snapshot-evidence`. |
| Native accessibility tree normalization. | Covered by `snapshot-evidence`. |

## Equivalence Tests

Characterization tests live in `src/test/characterization.test.ts` and cover:

- `findAxeCli` lookup command, timeout, and null behavior.
- `describeNativeUi` command arguments and exact timeout/buffer/reject options.
- Non-rejecting error normalization.
- `execFilePromise` rejection behavior with attached stdout/stderr.
- Optional stdin forwarding for command-runner parity.

Current result:

```text
6 passing, 0 failed, 0 skipped, 0 todo
```

## Follow-Ups

- Final CLI composition should pass this adapter into `snapshot-evidence` as `findAxeCli` and `describeNativeUi`.
