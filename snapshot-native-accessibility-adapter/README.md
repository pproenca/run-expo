# snapshot-native-accessibility-adapter

Native accessibility process adapter for `snapshot-evidence`.

It preserves the legacy `commandPath("axe")` lookup and
`axe describe-ui --udid <deviceId>` execution options used by snapshot fallback
capture.
