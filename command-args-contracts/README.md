# @expo98/command-args-contracts

Modernized command argument contract vocabulary for expo98.

This package exposes the legacy `CommandArgsByName` surface as runtime data:

- command names in legacy argument-map order
- positional ref-action command names
- action and kind vocabularies used by command argument unions
- command-to-argument-type contract lookups

## Verification

```bash
npm test
```

