# @expo98/interaction-trace-expression

Modernized Hermes interaction trace `Runtime.evaluate` expression extracted
from `legacy/expo98/dist/expo-ios.mjs`.

The package preserves the runtime expression used by the `trace` command to
install/read/clear/stop a React commit and animation-frame tracer in the app
runtime.

Run:

```bash
npm test
```
