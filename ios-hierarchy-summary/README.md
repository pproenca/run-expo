# @expo98/ios-hierarchy-summary

Typed transformation of iOS hierarchy summary behavior from `legacy/expo98/dist/expo-ios.mjs`.

The module wraps `axe describe-ui --udid` and summarizes hierarchy trees into counts, bounds, role totals, sample labels, and UX review insight text.

